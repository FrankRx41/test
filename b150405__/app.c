#include<stdio.h>
#include "mylib.h"
int main(void)
{
   int val;
   int x,y;
   x=12;
   y=18;
   val=add(x,y);
   printf("the mult of x and y is %d\n",val);
   val=func(100);
   printf("the sum is%d \n",val);
   //a
}
